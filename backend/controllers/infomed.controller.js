const inforMedication = async (req, res) => { 
    // route to get the medicine 
    res.json({ 
        Response: 'Route to get user medicine calls external service api. '
    }); 
};  


module.exports = inforMedication; 